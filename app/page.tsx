export default function Home() {
  return (
    <>
      <style>{`
        *{margin:0;padding:0;box-sizing:border-box;}
        :root{
          --navy:#1B3A5C;--navy-dark:#0F2540;--navy-mid:#2B5080;
          --blue-light:#A8C5DA;--off-white:#F0EEE9;
          --text-dark:#111827;--text-mid:#4B5563;
          --border:rgba(27,58,92,0.12);
        }
        html{scroll-behavior:smooth;}
        body{font-family:'Montserrat',sans-serif;background:#fff;color:var(--text-dark);overflow-x:hidden;}
        
        /* TODO EL CSS DE TU index.html ACÁ */
      `}</style>

      {/* TODO EL HTML DE TU index.html ACÁ */}
      <nav className="nav">
        {/* ... */}
      </nav>
      {/* resto del contenido */}
    </>
  );
}
